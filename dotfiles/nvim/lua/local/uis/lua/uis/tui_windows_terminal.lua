vim.notify("setting up for running under Windows Terminal", vim.log.levels.DEBUG)

-- NOTE::IMPROVEMENT it would be nice if the font-switching function could be
-- overridden to change the font on the fly in this specific tab of this window
